export interface IInjectorOptions {
  /** Debugger mode on / off, default false */
  debug?: boolean;
  /** All injectable classes must be mentioned in providers, default true */
  strictProviders?: boolean;
}
